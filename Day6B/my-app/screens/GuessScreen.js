import { View, Text } from "react-native";

const GuessScreen = ({ userNumber }) => {
  return (
    <View>
      <Text>GuessScreen {userNumber}</Text>
    </View>
  );
};

export default GuessScreen;
