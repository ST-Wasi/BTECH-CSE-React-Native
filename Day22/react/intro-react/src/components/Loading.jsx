import React from 'react'
import { useState } from 'react'

function Loading({ count, setName }) {

    return (
        <div>
            <h1>Loading {count}</h1>
        </div>
    )
}

export default Loading
